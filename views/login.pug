<!DOCTYPE html>
html(lang="en")
    head
        head
        include partials/header
    body.login
        .contenedor-formulario
            h1 Iniciar sesión en UpTaskMM
            if locals.errors
                .alertas
                    each cat in Object.keys(locals.errors)
                        each error in errors[cat]
                            .alerta(class=`${cat}`)
                                p= error
            form#formulario.caja.caja-login(method="POST" action="/login")
                .campo
                    label(for="email") E-mail:
                    input(type="email" name="email" placeholder="E-mail" value= email)
                .campo
                    label(for="password") Contraseña:
                    input(type="password" name="password" placeholder="Contraseña" )
                .campo.enviar
                    input(type="submit" value="Iniciar sesión" class="").boton
                .campo.acciones
                    a(href="/newAccount") ¿No tenés una cuenta? Registrate
                    a(href="/restore") Olvidé mi contraseña
        
        script(src="/dist/bundle.js")